# Note to Devs - coinbase/gdax shut down their test environment with no prior warning.
#                therefore this section is largely redundant but left in place in case
#                you wish to test/trade using an alternative configuration.
gdax:
  api:
    baseUrl: "https://api.gdax.com/"
  key:
  secret:
  passphrase:
  defaultProduct: "BTC-GBP"

websocket:
  baseUrl: "wss://ws-feed.gdax.com/"
  enabled: false

liveorderbook:
  defaultProduct: "BTC-GBP"
  timeout: 15

gui:
  enabled: false
